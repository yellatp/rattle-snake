# Rattle-Snake V2 — self-hosted deployment (api + web, optional ollama).
#
#   docker compose up --build              # api + web
#   docker compose --profile llm up --build # + local Ollama for LLM_*_URL=http://ollama:11434/v1
#
# Then open http://localhost:4321

services:
  api:
    build:
      context: .
      target: runner
      args:
        TARGET: api
    image: rattlesnake/api:2.0.0
    ports:
      - "8787:8787"
    environment:
      API_PORT: "8787"
      LLM_PROVIDER: "${LLM_PROVIDER:-openai}"
      LLM_BASE_URL: "${LLM_BASE_URL:-http://localhost:11434/v1}"
      LLM_API_KEY: "${LLM_API_KEY:-ollama}"
      LLM_MODEL: "${LLM_MODEL:-llama3.1}"
      LLM_TEMPERATURE: "${LLM_TEMPERATURE:-0.3}"
      DEBATE_CROSS_TALK_ROUNDS: "${DEBATE_CROSS_TALK_ROUNDS:-2}"
      AGENT_MAX_RETRIES: "${AGENT_MAX_RETRIES:-2}"
      DATABASE_PATH: "/app/data/rattle-snake.db"
      CORS_ORIGINS: "${CORS_ORIGINS:-}"
    volumes:
      - rattle-data:/app/data
    restart: unless-stopped

  web:
    build:
      context: .
      target: runner
      args:
        TARGET: web
    image: rattlesnake/web:2.0.0
    ports:
      - "4321:4321"
    environment:
      PORT: "4321"
      HOST: "0.0.0.0"
      PUBLIC_API_URL: "${PUBLIC_API_URL:-http://localhost:8787}"
    depends_on:
      - api
    restart: unless-stopped

  ollama:
    profiles: ["llm"]
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama-models:/root/.ollama
    restart: unless-stopped

volumes:
  rattle-data:
  ollama-models:
